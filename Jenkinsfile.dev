pipeline {
    agent any
    stages {
        stage('Build') {
            steps { sh './gradlew build -x test' }
        }
        stage('Deploy Local') {
            steps { sh 'docker-compose up -d' }
        }
    }
}
